# HANDOFF - Skyrim SE build, 2026-08-27

Supersedes HANDOFF-2026-08-26.md. That handoff's mandated audit ran 2026-08-26
(23-agent workflow wf_35880592-44a, 137 findings, 12 adversarially
cross-checked): every environment claim verified, four doc staleness clusters
fixed, and the SKSE blocker root-caused and FIXED. Every claim below has a
receipt.

## Mission

Curated stable Skyrim SE modlist on the LATEST runtime, fully AI-driven and
headless: the user picks mods (curator extension keeps), Claude does all
modding; Steam Play launches the full MO2 modlist. Pillars: Proteus
multi-character campaign (AT RISK - see parked), Community Shaders renderer
(parked upstream), CBBE Curvy + Reverie / HIMBO + SkySight, FSMP cloth-only
(decided, not installed), Skyrim Unbound alternate start.

## Standing rules (user directives)

All of HANDOFF-2026-08-26 rules 1-9 still stand, plus:

10. **NO POPUPS, EVER (2026-08-26, stated with force).** Every failure path in
    every tool must log to a file instead of showing a dialog. If a component
    can pop a modal, patch it out in our fork.
11. **We maintain Ensrick forks of ALL tools we ship; patches live on fork
    branches, not as loose working-tree diffs.** Current fork branches:
    Ensrick/skse64 ensrick/headless-log-only, Ensrick/SKSE64Plugins
    ensrick/1.7.99-headless, plus the toolchain.json forks (modorganizer,
    libloot, lootcli, Synthesis, Spriggit, TES5Edit, houseCARL).

## State (2026-08-26 ~01:00, all receipts on disk)

- Game 1.7.99.0. Game root runs SELF-BUILT SKSE from ianpatt/skse64 master
  @14db212 + log-only patches: loader FE4AD0F0, dll 7536E8DD, PDB deployed.
  Official 2.3.0 preserved as .bak.v2.3.0-nexus. WHY: official 2.3.0 was
  compiled with pre-1.7.99 PlayerCharacter offsets - GetNthTintMaskColor
  indexes a stale-offset NULL tintMasks -> the +0x420D0 crash; fix committed
  upstream (6498c52) but UNRELEASED. Full chain: records/
  selfbuild-2026-08-24-skse-skee64.md Addendum 5.
- First full-chain launch on the rebuilt pair: STABLE, 58 plugins, 11 SKSE
  dlls loaded, 0 refused, no popups, no crash (skse64.log 2026-08-25 19:34).
  IN-GAME New Game pass (Skyrim Unbound start scene) = the remaining
  acceptance gate; user was in session at handoff time.
- Profile: 50 enabled / 15 parked mods, 58 starred plugins, zero missing
  masters, zero order violations. Ledger records/installed-mods.json = 65
  entries covering every mod dir 1:1, enabled flags synced, all sha256s
  describe real on-disk archives (24 re-recorded after the audit found they
  hashed deleted transient repacks).
- UNPARKED 2026-08-26: PapyrusUtil 4.7 (official 1.7.99, Nexus file 795182,
  landed 23:42Z - unblocks Skyrim Unbound/NFF/CBBE-morph scripts).
  JContainers 4.3.1 (author GitHub prerelease) installed then RE-PARKED:
  skse64 master's plugin gate refuses it pending an AddressLibraryV5
  declaration (rebuild it from source with the flag, or wait for re-release).
- Archive store moved %TEMP%\modassets -> mo2-instances\skyrim-se\downloads
  (96 files, 16.4 GB; modasset.py CACHE updated). ~100 headless stage-dir
  leftovers moved into .mo2-headless-trash (13.5 GB; deletion = user's call).
- Saves: ~90 stale 2020-2025 saves moved to Saves.pre2026-archive; Steam
  Cloud DISABLED for 489830 (localconfig.vdf cloudenabled=0, backup
  .bak.v2026-08-26) so they cannot resurrect or conflict-dialog a launch.
- Skyrim.ini: sTestFile1-3 removed, Papyrus logging ON (for crash
  attribution; turn off when stable), FOV 120/120 injected (user wants
  120 FP / 90 TP - TP split needs a camera mod, research running).
- launch_skyrim.ps1: empty-seed abort guard, BOM-proof WriteAllLines, dead
  path line removed. launch_triage.py: --max-age-min freshness gate (exit 3)
  + crash-log-newer-than-skse64.log detection (exit 4).
- audit/update_sweep.py NEW: full-ledger Nexus radar (found PapyrusUtil 4.7
  minutes after upload). plugin_watch.py still covers the fixed page set.
- install_mod.py: --replace flow for mod updates (MO2Headless --replace),
  ledger dedupe by modName, verify() understands parked mods and
  deliberately-disabled plugins (Lux CC Myrwatch/Wraithguard - their fixer
  masters aren't installed). --verify = 0 problems.

## Parked (15) - unpark triggers

| mod | trigger |
|---|---|
| Community Shaders core | upstream PR #2674 (draft, crash reports open) merges/ships 1.7.99 build. 6 feature packs + Lux CS/Azurite III CS esps stay enabled; visuals look un-CS'd until then (user noticed - expected) |
| SPID | po3 tagged 7.3.3 upstream 08-25; release imminent - watch releases |
| SSE Display Tweaks | PORT round 3 in flight on Ensrick fork. Round-2 dll is DANGEROUS: on Load-failure unload its vendored framework VirtualFrees its SetBase slice of SKSE's SHARED branch pool - first-page pointer releases the whole 64KB region, SKSE AVs in the next hook commit (crash-2026-08-25-20-36-52.log, fully deterministic; memory reference_skse_shared_pool_unload_free). Affects the whole SlavicPotato ISKSEBase plugin family; two upstream issue drafts staged. Do NOT re-enable until the Destroy-ownership patch + IEvents 1.7.99 offsets + displacement ports all land. Meanwhile: fps solved anyway - the real cap was the 4K primary running 60Hz desktop (now 120Hz) + exclusive fullscreen; static Skyrim.ini fMaxTime=0.0083 covers physics |
| JContainers | rebuild w/ AddressLibraryV5 declaration or author re-release |
| MCM Helper | 1.7.99 build, or finish local dll-target rebuild (repos/MCM-Helper, NG 6.7.0 submodule ready) |
| OAR / Light Placer / CRD | author 1.7.99 builds (po3 wave); rebuild candidates, clones exist |
| Scrambled Bugs / Skill Uncapper / TNG | rebuild candidates (clones in repos/_rebuild_*); TNG also grab MCM hotfix 793745 at unpark |
| Bug Fixes SSE / EVLaS | closed source, authors silent - replacement evaluation if inactivity persists |
| Proteus | closed source, no build since 2024, author silent - PILLAR RISK, needs a user contingency decision (alternative: Multiple Characters of Skyrim 186444 teardown, never done) |
| Azurite III HDR / Darker Nights | superseded by Azurite III CS - not waiting on anything; delete or keep as rollback |

## Open work, in order

1. User's in-game New Game pass -> confirms the SKSE fix end-to-end (rule:
   nothing is "fixed" until user eyes). Papyrus.0.log + launch_triage.py
   after exit.
2. displaytweaks-port agent result -> install staged dll via install_mod
   pipeline, unpark, verify uncapped fps + havok fix at 143Hz. THEN set
   SkyrimPrefs iSize W/H 2560x1440 (currently 3840x2160 vs 1440p panel;
   edit only while game+MO2 are closed - the game rewrites SkyrimPrefs).
3. fov-research agent result -> in-game FOV control (script-only MCM
   preferred; SmoothCam 1.7.99 path + user's kept preset) for the 120/90
   split.
4. SKSE official 2.3.1 watch (plugin_watch.py) - when it ships, restore
   official pair, keep our fork branch for the popup doctrine (decide:
   re-apply log-only patches on top of each official release).
5. RaceMenu official 1.7.99 lands with the SKSE release -> replaces
   self-built skee64; overlay-disable stays until Bethesda fixes the NIF
   regression (no 1.7.100 hotfix as of 08-25).
6. Rebuild queue (fork doctrine): JContainers V5 flag, MCM Helper dll, then
   OAR/Scrambled Bugs/TNG/Light Placer/CRD as needed if authors lag.
7. Repo: main is ahead of origin by 80+ commits, audited clean of secrets -
   pushed 2026-08-26 (see git log).
8. Steam-side polish: Defender exclusion state unreadable without admin (one
   elevated Get-MpPreference, record in BASELINE); consider filing the MO2
   1.7.99 Plugins.txt issue upstream (needs user OK for outward post).

## Launch + triage procedure (unchanged core, hardened tools)

1. Kill ModOrganizer if running. 2. `pwsh -File audit/launch_skyrim.ps1`
(aborts on empty seed). 3. `py -3 audit/launch_triage.py --max-age-min 10`
after EVERY launch (exit 3 = never spawned, 4 = crashed after init, 2 =
plugin refusals). 4. User's eyes decide success: Skyrim Unbound start scene.
